## 1. Rail structure and visual behavior

- [x] 1.1 Add one picker-scoped variation flag and load rail assets only while it is enabled.
- [x] 1.2 Render shared accessible previous/next controls for recognized color and size rails.
- [x] 1.3 Add thin scrollbar, state-aware fades, and edge-centered chevrons using theme color variables.
- [x] 1.4 Tune color chips to 5.4rem and narrow/tablet size boxes to 5.6rem square.

## 2. Progressive enhancement

- [x] 2.1 Measure overflow and start/end scroll position to reveal only useful controls.
- [x] 2.2 Support grouped chevron scrolling, selected-value reveal, reduced motion, resize, and dynamically inserted rails.

## 3. Verification and documentation

- [x] 3.1 Verify color and size rails at desktop, tablet, and 390px touch widths; verify short and overflowing rows, both directions, selected-value reveal, unavailable/focus states, and no console errors.
- [x] 3.2 Verify the variation-off fallback restores wrapping colors and the responsive multi-row size grid with no rail controls or asset behavior.
- [x] 3.3 Record and archive the shipped capability in shared project documentation.
