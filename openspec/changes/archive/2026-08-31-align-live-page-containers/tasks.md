## 1. Container implementation

- [x] 1.1 Make the PDP follow the theme page width and align its desktop inset with the header.
- [x] 1.2 Align the desktop vertical-filter PLP shell with the header logo edge.

## 2. Live delivery and verification

- [x] 2.1 Push only the two changed CSS assets to active theme `148245381229`.
- [x] 2.2 Verify PDP and PLP geometry and rendered screenshots on the active storefront at desktop and mobile widths.

## 3. Shared documentation

- [x] 3.1 Record the direct-active-theme workflow in `CLAUDE.md` and synchronize the modified specifications.

## 4. Follow-up edge corrections

- [x] 4.1 Reject and roll back the PDP media-bleed trial; keep the full gallery chain on the shared 50px edge.
- [x] 4.2 Align the standalone Swish wishlist page with the header's desktop content edge without affecting cart/drawer cross-sells or mobile.
- [x] 4.3 Push only the affected assets/layout to active theme `148245381229` and verify live desktop/mobile geometry and screenshots.
- [x] 4.4 Restore the admin Media width control with Small 40/60, Medium 50/50, and Large 60/40; save Medium live and verify an exact 722px/722px split at 1600px.
- [x] 4.5 Remove the main-image `object-fit` override and verify the live Fill setting computes to `cover`.
