## Decision

Scope the rule to `.cart-drawer .product-option.cart-item__variant-options`, preserving the typography of custom line-item properties and any other product-option text. The size uses the same compact token as the drawer name: `calc(var(--font-heading-scale) * 1.4rem)`.
