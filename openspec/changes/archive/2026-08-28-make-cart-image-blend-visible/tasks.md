## 1. Image treatment

- [x] 1.1 Add a content-sized photo-frame wrapper around cart line-item images.
- [x] 1.2 Apply the PLP surface to that photo frame, leaving the media cell unstyled.
- [x] 1.3 Retain `multiply` blend mode and 1.6rem radius on the image.

## 2. Deploy and verify

- [x] 2.1 Push the two changed theme files to the active theme with an explicit `--only` list.
- [x] 2.2 Verify the blend visually on desktop and 390px mobile.
