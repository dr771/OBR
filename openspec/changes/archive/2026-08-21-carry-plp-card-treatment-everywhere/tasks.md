## 1. CSS scope lift

- [x] 1.1 In `assets/component-ob-swatches.css`, replace every `#ProductGridContainer .product-card-wrapper` selector (surface tint, `card__inner`/`card__media` blend, chip surface/border cues, meta typography, Wishlist King floating-heart restyle) with an unscoped `.product-card-wrapper` selector.
- [x] 1.2 Re-read the file after the edit to confirm no `#ProductGridContainer`-scoped rule was left behind unintentionally, and that no rule outside this block (facet/loading-feedback state, unrelated to this change) was accidentally touched.
- [x] 1.3 **Follow-up fix, found during live verification:** the naive `.product-card-wrapper` swap (2 classes) lost specificity against Dawn's own `.card .card__inner .card__media` rule (3 classes), which the `#ProductGridContainer` id had previously beaten outright — card photo `border-radius` silently fell back to 0px on *every* surface, including the collection grid. Fixed by bumping the wrapper selector to the compound `.card-wrapper.product-card-wrapper` throughout the block, and further to `.card-wrapper.product-card-wrapper .card__inner .card__media` for the specific rule that needed it. Re-verified via `getComputedStyle` on both the collection grid and the homepage featured surface: 16px (1.6rem) on both. See design.md Risks/Trade-offs.

## 2. Verify each rendering surface

- [x] 2.1 Push the changed file to the dev theme (`shopify theme push --theme=148245381229 --allow-live --only assets/component-ob-swatches.css`) and confirm the collection page and search results are visually unchanged (regression check).
- [x] 2.2 Verify the homepage featured-collection cards (https://original-brands-dev.myshopify.com/): surface tint, blended/borderless chips, surface-lightness selection cue with hairline border, rail overflow fades/chevrons (on a product with enough colors), tooltip, and meta typography all match the collection grid.
- [x] 2.3 Verify related-products cards on a PDP — **not currently exercisable**: `related-products.liquid` renders Shopify's native async `<product-recommendations>` component gated on `recommendations.performed && recommendations.products_count > 0`; with only 11 test products and no purchase/view history, the recommendations API returns empty and the section renders nothing on every product checked. Same class of caveat already documented for predictive search (`>8 results`) and PLP load-more (`>18 products`) — must be retested once there's a real catalog and traffic history. The CSS fix is in place; nothing left to do here until then.
- [x] 2.4 Verify collage-block cards on the homepage — **not currently exercisable**: no template in `templates/*.json` currently uses a `collage` section (`grep` found zero matches), so there is no live collage-block card to check yet. The CSS fix applies to it the moment a collage section is added.
- [x] 2.5 Verify a corrected brand's (Hi-Tec) card on the homepage featured-collection shows the same brand-specific photo padding as the collection grid — confirmed. Holster isn't in the current 11-product test catalog's featured selection to spot-check separately, but its correction is the same class-scoped CSS as Hi-Tec's, already unscoped along with everything else in this file.
- [x] 2.6 Spot-check Wishlist King's floating heart position/size on featured-collection cards — consistent circle, size, and position with the collection grid. (Related-products unverifiable per 2.3.)
- [x] 2.7 Check mobile viewport (~390px) on the featured-collection surface — 2-up grid, surface tint, and chip rail all render correctly. (Related-products/collage unverifiable per 2.3/2.4.)

## 3. Documentation

- [x] 3.1 Update the `## Purpose` line of `openspec/specs/plp-card-swatches/spec.md`, `openspec/specs/plp-brand-card-treatment/spec.md`, and `openspec/specs/plp-card-meta/spec.md` to describe the card treatment as applying to every rendering surface, not just "the PLP product card" (direct edit — Purpose is not carried by a delta for an existing capability).
- [x] 3.2 Update playbook "Next up" item 5 in `MIXED-SHOPS-PLAYBOOK.md` to record this as done, and add a shipped-capability line to CLAUDE.md's Current Status section.
