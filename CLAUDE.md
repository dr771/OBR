# CLAUDE.md

This file provides guidance to Claude Code when working with code in this repository. Codex also works in this repo and reads `AGENTS.md`, which points back here — CLAUDE.md is the shared source of truth for both, not Claude-specific.

## Project Overview

Shopify theme rebuild for Original Brands (originalbrands.nl) — multi-brand apparel + footwear retailer, migrating from a custom Drupal-ish CMS.

- **Store:** original-brands-dev.myshopify.com (storefront password: `original`)
- **Theme base:** Dawn (cloned fresh 2026-08-10, no upstream git history)
- **Product data source:** Akeneo, synced by Nick (connected — see Current Status for what's in the store)
- **Reference project:** SweatyBetty — same Dawn/Akeneo/OpenSpec stack, further along. Sibling directory of this repo (`../SweatyBetty`). Reuse its shipped patterns and lessons, not its SB-specific branding/hacks.
- **Two machines:** this repo is worked on from **both** a macOS box (`~/Projects/OriginalBrands`) and a Windows box (`C:\Users\rezni\SHOPIFY\OriginalBrands`). Neither is "the old one". Prefer machine-neutral references in docs — `../SweatyBetty` resolves correctly on both, absolute paths don't. Pull before starting; the other machine may be ahead.

## Project Docs

Read these before any major decision:

- [MIXED-SHOPS-PLAYBOOK.md](MIXED-SHOPS-PLAYBOOK.md) — every scoping decision for Original Brands: reuse ledger against SB's shipped specs, live-site audit, homepage direction, Akeneo→Shopify product-model decision. Read this first, always.
- [MIGRATION-TO-LIVE.md](MIGRATION-TO-LIVE.md) — launch checklist for the dev→live **store-to-store** migration (two separate shops, per Nick's Akeneo setup).
- [NICK.md](NICK.md) — open Akeneo/sync data issues to raise with Nick. Don't re-report these as if new.
- [CI-STYLE-TOKENS.md](CI-STYLE-TOKENS.md) — reusable cross-component design tokens (ink/accent colors, button component, link conventions, dense-UI heading rules, the cart-drawer font-family gotcha). Check before re-deriving typography/color values a new component needs — extracted from PDP/footer/PLP/cart-drawer work already shipped, not a spec.
- `openspec/specs/` — capability specs, seeded on-touch as work starts (empty until the first change is proposed/archived)
- `mockup/` — the pre-Shopify static homepage mockup, reference only, not the live theme

## Shopify CLI

**Direct-active-theme workflow — hard rule.** This dev shop's active main Dawn theme
`148245381229` is the working surface agents must edit, push to, and verify against.
In this project, “live” means that active theme on
`original-brands-dev.myshopify.com`, even though the shop itself is the non-public dev
shop. Push each requested theme-code change directly to that theme with an explicit
`--only` list, then verify the real storefront URL in Chrome.

```
shopify theme push --store=original-brands-dev.myshopify.com --theme=148245381229 --allow-live --only <file>
```

Do **not** use `shopify theme dev`, localhost, a preview theme, or a CLI Development
theme as the normal implementation/verification loop. Their settings and assets can be
out of sync with the active theme and therefore give false results. Only use one when the
owner explicitly requests a separate preview; stop it afterward, never report it as live
verification, and still make the approved change on theme `148245381229`.

**Store-identity gate for admin writes.** Before every Shopify Admin API mutation, retrieve the connected shop and proceed only when it is **Original Brands DEV** with the exact domain `original-brands-dev.myshopify.com`. This protects the shared connector from an accidental shop switch while keeping collection, navigation, metafield, and other shop-data work in the Admin API.

## OpenSpec CLI

The Hard Rules below assume `openspec` is on PATH. The npm package is **`@fission-ai/openspec`** — *not* `openspec` (that name is an unrelated 2019 stub at v0.0.0, and `openspec-cli` / `@openspec/cli` don't exist). On a fresh machine:

```
npm install -g @fission-ai/openspec
openspec list --specs          # sanity check, run from the repo root
```

## Centralized `ob-*` snippets

Akeneo/metafield interpretation lives in these, never inline in a template (mirrors SB's `sb-*` convention; enforced by the `akeneo-option-handling` spec):

- `ob-option-meta` — option kind from the bracketed Akeneo key (`[color]` → color, `[shoe_size_eu]` → size). Never branch on a visible/translated label.
- `ob-variant-color-code` — a selected variant SKU's Akeneo color code, normalized for media matching (`192-953` in SKU → `192_953` in filenames).
- `ob-media-color-code` — a media/image filename's Akeneo color code. **Codes can span multiple segments** (`192_953`); don't port SB's single-segment version.
- `ob-card-swatches` — PLP card swatch row: a single-row chip rail (hover swap + hover-pair data), with `ob-option-rail-controls` for its chevrons.
- `ob-option-rail-controls` — the previous/next chevrons for any single-row rail (PDP option pickers, PLP card chips); `class_root` picks which surface's styling they take.
- `ob-swatch-input` — one PDP color chip (image swatch from the variant's own photo).
- `ob-facet-color-chip` / `ob-facet-swatch-input` — color *filter* chip: flat hex from the `filtercolors` metaobject, deliberately not an image swatch.
- `ob-plp-sort-options` — the four approved collection sort choices in fixed order, with a hidden selected fallback when Shopify's current/default sort is outside the whitelist. Search sorting deliberately stays native.

Client behavior for the card swatches is in `assets/ob-card-swatches.js` (document-level delegation — Dawn replaces the grid wholesale on every facet change). Two files are shared by the PLP and PDP rather than per-capability: `assets/ob-option-rail.js` (rail overflow cues + chevron scrolling, for any `[data-ob-option-rail]` inside a `[data-ob-option-rail-shell]`) and `assets/ob-swatch-tooltip.js` (the one fixed-position chip tooltip both surfaces need, since a rail's scroll track clips a chip-anchored CSS tooltip). Collection load-more behavior is delegated from `assets/ob-plp.js`; facet loading feedback and corrective scroll clamping remain in Dawn's `assets/facets.js` response path.

## Hard Rules

- **No automated design/AI audit tools.** Do not run any automated design/AI audit against a reference (or anything else in this project) — the user does not want that tooling's recommendations in the loop right now. For a requested visual match, measure the named element directly instead.
- **Spec-covered changes:** For any bug report or behavior-change request, run `openspec list --specs` first, before touching code — don't rely on recognizing the capability from how the request happens to be phrased. If the touched area matches a listed capability, read its spec before editing. If the change would alter a documented SHALL/MUST requirement (not just an unspecified implementation detail), route it through `/opsx:propose` → apply → archive instead of editing the code directly. Early on, `openspec/specs/` will mostly be empty — as capabilities get built (many by porting from SB's reuse ledger), seed a spec for each one rather than skipping this because "there's nothing there yet."
- **Theme settings are protected deployment state.** `.shopifyignore` excludes `config/settings_data.json`, so normal `theme push`, `theme pull`, and `theme dev` operations cannot overwrite merchant settings. Change settings in Shopify Admin. To record them in Git, pull `config/settings_data.json` from the intended theme into a temporary directory that has no `.shopifyignore`, review the diff, then copy only the reviewed live file into this repo and commit it. Never remove the ignore entry for an ordinary deployment. A deliberate settings migration to another shop/theme is a separate operation: resolve both theme IDs, pull and back up the target settings first, review the full source/target diff, and only then push the file from a temporary theme directory after explicit user approval.
- **"Autopilot" workflow:** once intent is confirmed (via `/opsx:explore` + discussion), "autopilot" means running `/opsx:propose`/`/opsx:ff` → `/opsx:apply` unattended, then **stopping before `/opsx:archive`** to let the user review/correct the actual code — the archived spec should reflect the final corrected code, not a pre-review draft. Only archive after explicit approval. Only skip the pre-archive stop if the user explicitly says the autopilot run should include archive.
- **Shop-side dependencies go in the migration checklist, immediately.** This project ships by migrating to a *separate* live shop, so anything a feature depends on that lives in the shop rather than in this repo — an app config, a metafield/metaobject definition, a storefront-access setting, an admin toggle — must be appended to [MIGRATION-TO-LIVE.md](MIGRATION-TO-LIVE.md) *when you discover it*, not at launch. If a feature only works because of something you clicked in the admin, that's a checklist line.
- **"Update the docs" (end of a task):** check whether the task changed behavior for anything in `openspec/specs/` — if so, sync that spec too (a quick `/opsx:propose` → apply → archive cycle for a real requirement change, a direct edit for wording-only fixes). Also check whether MIXED-SHOPS-PLAYBOOK.md needs a new decision recorded (architecture/scoping calls go there, not into Claude memory — it's committed and readable by Codex too, see AGENTS.md).

## Current Status (2026-08-16)

First real build landed. Akeneo sync is live with **11 test products across 5 brands** (FitFlop, Hi-Tec, Holster, Loewenweiss, Sweaty Betty) — treat this as pipeline test data, **not** the live assortment (the SB items are apparel examples, per owner). At 11 products the predictive-search ">8 results" branch is finally exercisable live; PLP load-more still needs a collection past 18.

Shipped and verified on this shop's **main Dawn theme `148245381229`** (storefront password `original`), and also present on the Development theme used during the build. Note: this whole *shop* is the dev environment and never goes public under the real domain — see "Next up" item 4 — so pushing here is not a release and needs no special care.
- PLP card colour swatches — non-navigating pressed-state buttons select in place, retarget the card's PDP link, persist the selected first image, and reveal that color's matched second shot only over the image area (`plp-card-swatch-selection`). The color-matched second shot now wins on every grid regardless of the section's own `show_secondary_image` setting (`plp-card-swatches`, fixed 2026-08-12) — that setting is on for the homepage, related-products, and search-results sections but off for the collection page, so before the fix those three surfaces showed the product's generic default second image on hover instead of the selected color's own.
- PDP colour swatches (img-swatch chips)
- Colour filter (flat hex chips from `filtercolors`) presented as a five-column 2.8rem chip grid with hover/focus labels on desktop and a single horizontal chip row in the mobile filter bar; touch does not retain the tooltip.
- D1 card image tracks the first *available* variant
- **Size facet as a 4-column box grid** (`plp-size-facet-grid`), and the collection page switched to the **vertical** filter layout — it had been `horizontal`, under which Dawn does no show-more truncation at all
- **PDP option rails + size picker** (`pdp-size-picker-order`, `pdp-size-picker-grid`, `pdp-option-rails`, shipped + live-verified + archived 2026-08-12) — EU shoe sizes render numerically, tops/bottoms render in semantic letter order, raw Akeneo headings are replaced with `Maat`, and recognized sizes retain a skin-agnostic responsive box-grid fallback (four 5.6rem targets in narrow product columns, eight 4.8rem targets from 44rem). By default, recognized colors and sizes use independent single-row rails: 5.4rem color image chips, a thin scrollbar, edge fades, and directional chevrons only when more choices exist. The shared progressive enhancement preserves native radio/variant behavior, honors reduced motion, reveals a newly selected off-screen choice, and can be entirely disabled via `ob_single_row_options` in `snippets/product-variant-picker.liquid` to restore wrapping colors and the multi-row size grid. Live-verified with footwear and apparel at desktop, tablet, and 390px mobile, including selected, unavailable, keyboard-focus, overflow, both rail directions, and rollback behavior.
- **PDP per-color media galleries** (`pdp-color-media-gallery`) — main gallery, mobile counter, and expanded modal show only the selected color's photography; verified with both single-segment and normalized multi-segment codes
- **PLP grid UX bundle** — 18-item pages with native “Toon meer” append behavior, immediate geometry-stable skeleton feedback, corrective-only scroll clamping, and four collection sort choices; search sorting stays native. Because the test assortment has only 7 products, load-more success/failure/reset was verified against a synthetic real section response and must be retested naturally once a collection exceeds 18 products.
- **PLP filter experience bundle** (`plp-filter-panel-chrome`, `plp-mobile-filter-bar`) — desktop vertical facets are open-by-default “Shop op …” accordions with compact value-only pills/reset links and a repeatable sticky summon control; mobile collection/search pages replace Dawn's drawer with a persistent Type/Maat/Kleur bar above the grid. The three rows filter instantly through Dawn AJAX, while active parameters for omitted filters are preserved and remain pill-removable.
- **Cart drawer line-item layout** (`cart-drawer-line-item-layout`) — label-free `Value / Value` variant options, single line-total price, remove-control/price row swap with a compact X icon, content-sized quantity stepper, visually-hidden-but-accessible column headers, "Mijn winkelwagen" heading with a hairline separator, and a borderless asymmetric-gutter panel. Also flipped `cart_type` from Dawn's default `notification` to `drawer` (was otherwise unreachable) and fixed a store-wide add-to-cart blocker — see "Shop config gotchas" below.
- **Wishlist King (Swish) integration** (`wishlist-integration`, shipped + live-verified + archived 2026-08-12) — header heart + live count badge, PDP toggle beside add-to-cart, cart drawer/`/cart` wishlist cross-sell with a move-to-cart AJAX intercept (WK's own CTA does a full-page `POST /cart/add`; verified live and intercepted so it stays in-drawer), an Akeneo bracket-key option-parser patch, and a raw-key label overlay — all fail-open if WK is absent/not booted. The app's theme app embeds were off at proposal time and got enabled+saved live mid-session (all 5 Swish embeds); `assets/ob-wishlist.js` is one file for all WK-dependent JS, following the `ob-card-swatches.js`/`ob-plp.js` per-capability convention rather than porting SB's monolithic `custom-theme.js`. Live exploration found real deltas from SB's setup worth remembering: WK's `wishlist-page` embed renders dropdown option pickers here (not swatches), and `<wishlist-page>` always renders its own page-level chrome (`.wk-header` — "Mijn favorieten" h1 + login callout) that had to be hidden via CSS since no attribute suppresses it. The compact cross-sell uses equal 32px control heights, 55px size pickers, color widths measured from each product's longest option, and zero card-meta bottom margin.
  - **Cross-sell fixes for PLP-added items (shipped + live-verified + archived 2026-08-28).** An item added from a PLP card carries no variant, so it hits WK's placeholder/disabled render path — which was broken in four ways no PDP-added item ever reached. (1) Dawn's `cart-drawer-items` `change` listener (`assets/cart.js`) treated WK's option `<select>`s as quantity inputs and `setCustomValidity`'d them, so the WK form failed interactive validation and **never dispatched a `submit` event** — the move-to-cart intercept never ran and nothing reached `/cart/add`; fixed with a `.quantity__input` guard in `onChange`. (2) WK's own `.wk-cta-button[disabled]` rule (width:100%/45px) outspecified our plain descendant selector and squashed the pickers to 0px — the CTA's sizing declarations now carry `!important`, which is exactly why SB's `custom.css` is `!important` throughout. (3) `grid-template-columns: 8rem 1fr` → `minmax(0, 1fr)`, since a track's automatic minimum is its content's max-content size and the nowrap option row widened the card past the drawer. (4) The label overlay's MutationObservers died with the `<cart-drawer-items>` node the drawer replaces on every cart mutation; they now re-attach off a `document.body` observer. Also: Translate & Adapt strips the Akeneo brackets before the storefront (`options[shoe_size_eu]`, not `options[[shoe_size_eu]]`), so the raw key is read from `select.name` rather than matched as a bracket token, and a compact picker's placeholder is collapsed to the bare label ("Maat", not "Selecteer maat") so two unselected pickers don't both ellipsize to "Selecte…". Both label deltas and the no-variant layout/observer guarantees are now recorded in the `wishlist-integration` spec. Option-width measuring moved from canvas `measureText` to an off-screen ruler span — `measureText` ignores letter-spacing and came out ~3px short per label.
- **Predictive search overlay** (`predictive-search-overlay`, shipped + live-verified + archived 2026-08-12) — header predictive search reworked to an 8-result-max, 2-column desktop grid with 115x140 responsive imagery, a translatable uppercase "Suggested products" heading shared by the suggestions/pages groups, transparent hover with the native keyboard-selected highlight preserved, and a conditional "See all results" action (only past 8 matches; native "Search for …" at zero; nothing extra for 1-8). Also raises the header's stacking context while search is open so it can't be occluded by Wishlist King's collection-card hearts (`z-index: 9`), without disturbing the cart drawer's own precedence (`z-index: 1000`) — confirmed the two surfaces are mutually exclusive anyway (opening the cart drawer auto-closes search via Dawn's native disclosure handling). Ported 1:1 from SB with one structural OB delta: OB's `sections/predictive-search.liquid` wraps each result in `<product-component>` (a newer Dawn element SB's clone predates), which had to be preserved rather than dropped — the `> *` keyboard-highlight selector still resolves correctly since the custom element renders `display: block`. New CSS lives in `assets/component-ob-search.css` (OB has no `custom.css`, unlike SB). Live review also caught a shop-config gap the file diff missed: `settings.predictive_search_show_price` was `false` in OB vs. `true` on SB's live site — untracked in SB's repo (SB has no committed `settings_data.json`), so invisible to diffing; fixed by flipping the setting and pushing. The `>8` "see all results" branch can't be exercised live until the catalog exceeds 8 products — the current 7-product test catalog caps every query below that threshold, same caveat as the PLP load-more capability.

- **Per-brand card image treatment** (`plp-brand-card-treatment`, shipped + live-verified 2026-08-16) — every card carries `ob-brand--<vendor handleized>` + `data-brand` from the shared snippet, and each brand's photo correction is a custom-property override (padding per side, `object-fit`, tile background, blend) in `assets/component-ob-brand-media.css`. Shipped desktop-only (≥990px): Hi-Tec 18px, Holster 10px. The grid's shared warm surface + multiply lives in `component-ob-swatches.css` and is now `var(--ob-card-media-blend, multiply)` so a brand can opt out. See playbook D11 for the two traps (padding needs `contain`; don't paint over the existing multiply).
- **PLP card meta order + reference typography** (`plp-card-meta`, shipped + live-verified 2026-08-16) — brand label moved above the product name in the markup (not flex-reordered, so reading order matches), still gated by each section's `show_vendor`. Label 10px/600/15px/1.8px uppercase at 45% ink via colour alpha (not Dawn's `.light` opacity), name 16px/500/22px, 4px between them — all measured off the Bolt reference, whose card is 300.5px against OB's 301px, so values are 1:1. **`theme.liquid` now loads body 500/600 faces**: Dawn ships only regular + bold, and a declared weight with no face is silently remapped (500→400, 600→700), which computed-style diffing cannot see. Side effect: everything declaring 500/600 store-wide now renders truly, including the card price (was faux-700).
- **Desktop PLP shell/grid spacing matched to the approved Bolt reference** (`match-desktop-plp-spacing`, shipped + archived 2026-08-16; outer alignment superseded 2026-08-25) — at ≥990px: 50px page inset aligned with the header logo, 230px filter column, 48px filter-to-grid gutter, 24px/36px product-grid gaps. Only the former 24px outer inset changed; mobile, card internals, and non-collection grids remain untouched. See playbook D10.
- **PLP card colour chips are a single-row rail** (`plp-card-swatches` + `plp-brand-card-treatment`, shipped + live-verified + archived 2026-08-17) — chips no longer wrap: they ride a horizontal rail sized from the card so **five are fully visible and the sixth peeks** at any width (≈50px chips on the 301px desktop card, ≈27px at 390px). Overflow shows an edge fade plus chevrons that advance **one chip** each and are absent without hover. Selection is a difference in *surface* lightness (`plp-card-chip-surface-cue`, 2026-08-17): chips rest on `color-mix(in srgb, var(--ob-product-photo-surface) 40%, #fff)` and return to the full surface when active/hovered/focused, so the chosen colour is simply the darkest chip in the row. No border is drawn at rest — the transparent border width stays reserved only to keep the rail's geometry — but active/hover/focus-visible now additionally draw a hairline `0.1rem solid #66666612` border on top of the surface cue (`plp-card-swatch-border-cue`, 2026-08-21). Dimming the chip *image* instead was tried and rejected: it alters the content and swings from invisible on a dark packshot to washed out on a white one. Chip crops went 64px → 200px (and the PDP's 100px → 200px) because chips were being upscaled at 2×; PDP chips also went to a 0.7rem radius. Brand padding (D11) is now proportional — Hi-Tec 6%, Holster 3.3% — and applies to chips as well as tiles. Rail JS is shared with the PDP as `assets/ob-option-rail.js` (renamed from `ob-pdp-option-rail.js`, keyed on `[data-ob-option-rail-shell]`); rail CSS deliberately is not. Both surfaces now share the fixed-position `ob-swatch-tooltip.js`, because a scroll track's `overflow-y` is never `visible` and would clip a chip-anchored CSS tooltip. See playbook D12 for the four traps.
- **PLP card color chips moved below the main image** (`plp-card-swatches`, shipped 2026-08-16, spec backfilled 2026-08-17) — chips now sit under the product photo instead of after the price, share the card's warm surface color, and use `mix-blend-mode: multiply` like the main image instead of a bordered chip look; active/focus uses an outline, unavailable chips use reduced opacity instead of a dashed border. The same commit (2860b48) also matched card heading/price/badge typography to Bolt and restyled Wishlist King's floating collection-card heart — those two are still undocumented (no spec covers price/badge typography or the WK button treatment yet).

- **Finished PLP card treatment carried to every card surface** (`carry-plp-card-treatment-everywhere`, shipped + partially live-verified + archived 2026-08-21) — the D11/D12 swatch-chip, brand-photo-correction, and card-meta-typography rules in `assets/component-ob-swatches.css` were re-scoped from `#ProductGridContainer .product-card-wrapper` to a bare `.product-card-wrapper`, so every `card-product.liquid` render gets the identical treatment regardless of which section renders it (playbook "Next up" item 5, deferred since 2026-08-17 until PLP itself stabilized). The rail/tooltip JS (`ob-card-swatches.js`, `ob-option-rail.js`, `ob-swatch-tooltip.js`) needed no change — it was already document-delegated/attribute-keyed, not ID-scoped. **Trap:** the naive fix (swap the id prefix for `.product-card-wrapper`) silently dropped card-photo `border-radius` to 0px everywhere, including the previously-working collection grid — the id had given the old selector enough specificity to beat Dawn's own `.card .card__inner .card__media` rule outright, and a bare `.product-card-wrapper` (2 classes) doesn't. Fixed with the compound `.card-wrapper.product-card-wrapper` (both classes are genuinely on the same element, so this is real specificity, not a hack), matching Dawn's ancestor depth exactly where needed. Caught via `getComputedStyle`, not by eye — worth diffing computed styles, not just screenshots, after any id-scope removal. Live-verified on the homepage featured-collection surface: surface tint, borderless/blended chips, hover/selection surface-lightness cue, rail overflow chevrons, tooltip, Hi-Tec brand padding, and the mobile 2-up grid all match the collection grid exactly. Related products and collage blocks get the same CSS fix but aren't independently testable yet — related-products' native Shopify recommendation API returns empty with only 11 test products and no traffic history (same class of caveat as predictive search's `>8 results` branch and PLP load-more's `>18 products` threshold), and no template currently configures a collage section.

- **PDP matched to the Bolt reference** (`match-pdp-to-proto`, shipped + live-verified + archived 2026-08-21; container width superseded 2026-08-25) — the product page carried to the approved reference at measured fidelity: **118/118 sampled properties across 32 elements**, all icon/text centre-lines at 0. Six new capabilities (`pdp-layout-chrome`, `pdp-gallery-chrome`, `pdp-breadcrumb`, `pdp-usp-strip`, `pdp-size-guide-trigger`, `pdp-product-badges`) plus two modified (`pdp-option-rails` chips 5.4rem → 4.8rem, `pdp-size-picker-grid` boxes → a uniform 4.4rem). New: a breadcrumb, an editor-configurable two-item USP strip, a `Maatadvies` trigger (SB's `trigger`/`dialog` split, withheld until real tables exist), a bestseller badge in two placements off `custom.bestseller`, and the thumbnail chevrons relocated from Dawn's flanking positions into a bottom-right counter row (`Afbeelding X van Y`, driven by `assets/ob-pdp-gallery.js` off the active thumbnail — Dawn's own `.slider-counter` reports *pages*, so a 4-up rail of 7 images read "1 of 4"). All measured values live in `assets/component-ob-pdp.css`, scoped to `.ob-pdp`; the PLP was proven untouched by a 170-property before/after diff. **Deliberate departures from the reference** (owner's call, recorded as playbook D14–D16): no `Meer kleuren` label, no quantity stepper, no review badge on the image, wishlist heart stays beside add-to-cart (now a 5.6rem circle), the gallery image loses the reference's 40px/8px insets, the frame aspect is `1/1` not `1.06/1` because **every Akeneo image across all six brands is exactly 1:1**, and the USP's second line is `Voor 12u besteld, volgende werkdag geleverd` — the reference's `30 dagen gratis retour` was rejected as false, since the published policy withholds €5.95. **Superseding owner decision:** the PDP now follows the active theme's 1600px page width and uses the same 50px desktop content edge as the header/PLP. **Not done:** the reference's four detail accordions — the template ships a plain description block, so the treatment is styled but no panels are configured (their copy is merchant content that does not exist yet); the spec says so explicitly rather than promising them. See project memory `ob-dawn-pdp-override-traps` for the five Dawn rules that silently won.

- **PDP gallery and color chips carried to the PLP's blended-surface treatment** (`pdp-chip-plp-match`, shipped + live-verified + archived 2026-08-25) — owner-requested departure from the Bolt reference: color chips, the main gallery image, and the thumbnail rail all now share the PLP card-swatch look instead of their own white-surface/always-visible-border style. All three surfaces are borderless at rest with a `color-mix` translucent tint (a new `--ob-pdp-photo-surface` token, `#f1f5f9` — deliberately not `--ob-pdp-surface`, which is pure white and produced zero visible blend when tried first), `mix-blend-mode: multiply` on the photo, and a hairline `#66666612` border plus the surface returning to full tint on hover/active/focus/selection — no ink-colored border or ring anywhere. Chip size went `4.8rem` → `5.0rem` to match the PLP's typical resolved size; chip radius (`0.8rem`) and the main image/thumbnail radii are unchanged. Two Dawn base-CSS bugs surfaced and got fixed along the way: `.thumbnail:hover { opacity: 0.7 }` was dimming thumbnails on hover (chips never dim), and Dawn's `.thumbnail[aria-current]` family draws its active state as a `box-shadow` ring across four pseudo-state selectors of identical specificity to the override — including the mouse-click case (`:focus:not(:focus-visible)`, which matches on every click since buttons don't get `:focus-visible` from a click) — so all four had to be named explicitly with `box-shadow: none` or one would win by source order and redraw the old ring. A related latent bug in the shared `component-ob-swatches.css` `:checked` chip rule (`border-width: 0.2rem` surviving a `border-color`-only override) was also fixed. Modified `pdp-option-rails` (chip requirement) and `pdp-gallery-chrome` (main image + thumbnail requirements).

- **Footer built from stock Dawn's empty configuration and matched to the Bolt reference** (`footer-navigation` + `footer-proto-chrome`, shipped + live-verified + archived 2026-08-24) — the footer previously had `"blocks": {}` in `sections/footer-group.json` (no menus, no brand column, nothing clickable). Now wired to 3 real Shopify navigation menus (`footer-klantenservice`, `footer-over-ons`, `footer-handige-links`, 11 links total, all resolving to real Pages/routes/collections) plus a `brand_information` block reusing the site's existing logo file and a measured tagline. 6 new placeholder Pages were created for link targets that didn't exist yet (Contact and Merken were reused as-is). Visual design is measured against the proto's `getComputedStyle` output (not eyeballed) in a new `assets/component-ob-footer.css` scoped to `.ob-footer`, verified across desktop/tablet/390px mobile: background, the 2/4/5-column responsive grid with the brand column always spanning 2, heading/link typography and hover color, circular social-icon buttons. **Deliberate departures:** no gift-card link yet, Dawn's native automatic payment icons kept over the proto's static badges, Dawn's country/language selectors kept as native bottom-bar content (the proto has none to match against). **Two traps hit and fixed:** converting Dawn's flex-based `.footer__blocks-wrapper` to real CSS Grid (needed for the proto's exact breakpoints) collided with Dawn's own `.grid__item { width: calc(25% - ...) }` — meant for its flex-wrap layout, that percentage silently resolved against each item's own grid track once it became a grid child, collapsing every column to ~48px; fixed by resetting `width`/`max-width`/`flex` to `auto`/`none`/`none` on `.footer-block`. Separately, Dawn's own `@media (max-width:749px) { .footer-block.grid__item { margin: 4rem 0; } }` (written for its stacked mobile layout) was never neutralized and stacked on top of the grid's own row gap, producing oversized vertical gaps on mobile — caught via user screenshot after initial ship, fixed with `margin: 0` on `.footer-block` within `.ob-footer`.

- **Header announcement bar gets optional per-message icons and a breakpoint-aware layout** (`header-announcement-bar`, shipped + live-verified + archived 2026-08-25) — matched to the approved Bolt reference's 3-message trust bar (shipping/returns/authenticity, each with an icon), with an owner-approved departure from the reference's own behavior: the proto is a pure 2-state CSS hide/show (1 message <640px, all 3 from 640px+, never a carousel), but this theme keeps Dawn's existing announcement-bar scroller for mobile (1 visible, unchanged) and tablet (2 visible, new — `.grid__item` at 50% width via a scoped stylesheet, not Dawn's shared `grid--2-col-tablet` utility), going to a static all-visible centered row only at desktop (≥990px), with no scrolling/buttons/autoplay motion. Icons are a curated select (`snippets/ob-icon.liquid`, shared with `pdp-usp-strip`, extended with a new `refresh-cw` case) rather than an image upload, guaranteeing pixel-identical icons theme-wide. New `assets/component-ob-announcement-bar.css`. **Two bugs caught in live review, both fixed:** the message text was inheriting the `h5` class's serif heading font instead of the proto's sans body font (needed an explicit `font-family`/`font-weight` override); and Dawn's `auto_rotate` autoplay timer (on live for this section) kept firing at desktop width — its `scrollTo` call is a harmless no-op with nothing to scroll, but the fade-transition classes it also applies default a non-"current" message's opacity to 0, silently hiding it with no scroll ever happening to restore it, fixed with a desktop-only opacity override verified stable through a full autoplay cycle. Owner declined adding a new color scheme to match the reference's exact navy — the live section keeps the theme's existing dark `scheme-3`. The live theme already had 3 real messages configured before this change shipped (not the stale placeholder in the repo); `sections/header-group.json` was re-pulled and merged rather than overwritten, so no merchant edits were lost.

- **PLP card title hover swapped for an image zoom** (shipped + live-verified 2026-08-27) — removed the `underline-links-hover` class from both `card-product.liquid` card-wrapper markups (the only thing that class did; confirmed via grep, no other effect anywhere in the theme), so hovering a card no longer underlines the product title. In its place, `assets/component-ob-swatches.css` scales the `<img>` itself to `1.04` on `.card-wrapper.product-card-wrapper:hover` at ≥990px, applied to every state in the existing color-swap crossfade (single image, the static secondary image, and the JS-injected `.ob-card-img2` hover pair) so whichever image is visible always zooms. **Trap hit and fixed:** the first attempt scaled the `.media` wrapper instead of the `<img>` — `transform` on an element creates a new stacking context, so scaling `.media` orphaned the img's `mix-blend-mode: multiply` from `.card__media`'s tint backdrop (`isolation: isolate`), and the photo rendered plain white on hover instead of blended. A second attempt scaled `.card__media` itself (blend-safe, since it's already the isolation root) but visibly grew the whole card frame, not just the photo. The fix scales the `<img>` directly — blend-safe (a leaf's own transform doesn't change which backdrop its blend composites against) and frame-safe (`.card__media`'s `overflow: hidden` from Dawn crops the enlarged photo without moving the frame). Also preserved a pre-existing detail: the first/paired image's `transition: none` (added to kill flicker from Dawn's generic opacity transition once the lazy JS pair exists) had to become `transition: opacity 0s, transform var(--duration-long) ease` — opacity stays instant, transform now animates. No capability spec covers card hover/title-underline behavior yet (checked `plp-card-swatches` and `plp-card-meta` — neither documents it), so this was a direct edit, not an OpenSpec change; seed one if the treatment gets any more elaborate than this.

- **Sticky mobile checkout CTA on `/cart`** (shipped + live-verified 2026-09-02) — below 750px the checkout button leaves the redesigned summary card and pins to the bottom of the viewport (Holster's `/cart` pattern, measured off `holsteraustralia.eu/cart`): white bar, `#66666612` hairline top border, `0 -0.6rem 1.8rem rgba(0,0,0,.06)` shadow, `1.2rem 1.5rem` padding (`.page-width`'s own mobile gutter) plus `env(safe-area-inset-bottom)`, full-width button, z-index 9. Always fixed — no scroll listener, no reveal/dock, pure CSS in `assets/component-ob-cart-page.css`'s existing `max-width: 749px` block. Two things Holster gets wrong are fixed here: it has no safe-area padding, and its intended `.cart { padding-bottom: 140px }` compensator matches no element so the bar permanently covers the footer's last line — here `body:has(#main-cart-footer:not(.is-empty))` pads the document by the bar's exact 6.9rem, self-cancelling on an empty cart (cart.js toggles `.is-empty` live) with no dead space and no reload. **Trap:** Dawn's `isolate` class on `.cart__footer` is `position: relative; z-index: 0` — a stacking context that traps the fixed bar at level 0 and lets the site footer paint over it; `position: relative` does *not* create a containing block for `position: fixed`, so only the z-index needs neutralizing (`z-index: auto`, mobile only). z-index 9 is chosen to sit above Wishlist King's cross-sell hearts (also 9, earlier in the DOM, so source order wins) and below the sticky header (10), the cart drawer (1000) and the quantity-rules modal (1001) — drawer coverage verified by hit-test, not by eye.
- **`/cart` summary card: total back in its own row, out of the button** (2026-09-02) — the redesign had moved the total into the checkout button and dropped the totals row, which left the card claiming to be a summary of nothing and, worse, put the only copy of the total **outside** `.js-contents` — the region `cart.js` re-renders — so a quantity change updated the cart and left the button reading the old amount (live-reproduced at €430 vs a real €540). Both are fixed by putting the total back where Dawn had it: a `Geschat totaal` / `€430,00` row **inside** `.js-contents`, which refreshes natively with no JS at all (an earlier fix that patched `CartItems.getSectionsToRender()` to re-render `.cart__ctas` was reverted along with it — `assets/cart.js` is stock again). The row is deliberately **not** Dawn's centred serif: label and value are both Inter 600 (`1.5rem` / `1.8rem`), laid out `space-between`, with `0.12`-alpha hairlines above and below — the cart is an Inter surface per CI-STYLE-TOKENS, and the h2 label would otherwise inherit Fraunces. The visible tax note returns under it (Codex had captured that copy into the button's `title` tooltip). The page's checkout button is now a plain `Afrekenen`; **the cart drawer's button keeps its own `Afrekenen • €430,00`** — deliberate asymmetry, since the drawer has no totals row to state it. Same pass: the discount input takes the pill radius of the `Toepassen` button beside it (`999.9rem`, matching heights at 4rem) and that button drops Dawn's ~1px tracking to `normal`; both live in the shared `component-discounts.css`, so the cart drawer's discount box gets them too.

Thirty capabilities are seeded or built in `openspec/`. Archived changes are under `openspec/changes/archive/2026-08-11-port-akeneo-facets-swatches/`, `.../2026-08-11-plp-size-facet-grid/`, `.../2026-08-11-pdp-size-picker-order/`, `.../2026-08-11-pdp-color-media-gallery/`, `.../2026-08-11-plp-card-swatch-selection/`, `.../2026-08-11-port-plp-grid-ux-bundle/`, `.../2026-08-11-plp-color-filter-grid/`, `.../2026-08-11-port-plp-filter-experience/`, `.../2026-08-11-port-cart-drawer-line-item-layout/`, `.../2026-08-12-wishlist-integration/`, `.../2026-08-12-port-predictive-search-overlay/`, `.../2026-08-12-fix-plp-card-swatch-hover-image/`, `.../2026-08-12-pdp-size-picker-grid/`, `.../2026-08-12-pdp-option-rails/`, `.../2026-08-16-match-desktop-plp-spacing/`, `.../2026-08-16-plp-card-chip-placement/`, `.../2026-08-17-plp-card-swatch-rail/`, `.../2026-08-17-plp-filter-panel-proto-match/`, `.../2026-08-21-plp-card-swatch-border-cue/`, `.../2026-08-21-carry-plp-card-treatment-everywhere/`, `.../2026-08-21-match-pdp-to-proto/`, `.../2026-08-24-footer-proto-match/`, `.../2026-08-25-pdp-chip-plp-match/`, `.../2026-08-25-announcement-bar-icons/`, and `.../2026-08-28-wishlist-cross-sell-no-variant/`.

**Shop config gotcha — shipping zone vs. market mismatch (fixed 2026-08-11).** The dev shop's only shipping zone covered the US while its only configured Market is EU (BE/NL/DE/FR/LU) — no zone covered any country a real buyer could select, so *every* shippable product read as sold out (`available: false`, `/cart/add.js` 422) regardless of stock or inventory settings, store-wide. Fixed by adding a real "EU" shipping zone/rate to the default delivery profile. This is a Shopify default a fresh/copied shop can silently reintroduce — checklist item added to MIGRATION-TO-LIVE.md. See project memory `ob-dev-store-nothing-addable-to-cart` for the full diagnosis and an API gotcha (`deliveryProfileUpdate` needs `locationGroupsToUpdate`, not `profileLocationGroups`, which silently no-ops).

**OpenSpec CLI:** `npm install -g @fission-ai/openspec` — the npm name `openspec` is an unrelated stub. See the OpenSpec CLI section above.

**Not yet done:** the 2026-08-16 design meeting resolved the CTA color (blue `#38B6FF`, not red) and approved Bolt as the reference for live PLP/PDP typography, spacing, shadows, borders, and radii — but homepage composition stays deliberately open (owner, 2026-08-17). `mockup/index.html`'s default palette was flipped to match the confirmed color; the homepage itself isn't blocked on anything right now, it's just not being worked yet. See "Next up" in MIXED-SHOPS-PLAYBOOK.md.

**Theme workflow.** Shopify CLI is authenticated on this machine:
```
shopify theme push --theme=148245381229 --allow-live --only <files>   # main Dawn theme
```
Always push with `--only <changed files>`. `.shopifyignore` independently blocks `config/settings_data.json`, including when it is accidentally named in `--only`; do not remove that protection for a normal deploy. JSON templates remain merchant-editable state and must be pulled/diffed immediately before an intentional template push.

**Use the active main theme, not a CLI Development theme.** Development themes are both
out of workflow and unreliable here: they are ephemeral, machine-bound, and can carry
settings/assets that differ from the active theme. The active storefront is the source of
truth for every visual verification.

**Never hand-encode files through Admin GraphQL `themeFilesUpsert`** — that corrupted `card-product.liquid` earlier in this project. Use the CLI.
