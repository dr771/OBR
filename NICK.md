Three data bugs in Nick's feed (not theme)
"Black Grey" is tagged blue+grey instead of black+grey — that's why the SB sweatshirt looked "wrong" under the blue filter. The theme behaved correctly on bad data.
brown hexcode is #FFD700 — gold. Brown chip renders gold.
Filter labels show English code ("brown") not Dutch label ("bruin") — metaobject display-name config, wrong for a NL storefront.